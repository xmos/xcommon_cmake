Static library has additional source files, one for each supported language, in the directory
named src. Compiler flags are also specifed for these additional source files. Compilation of the
application will fail if these sources are not compiled with the correct flags.
