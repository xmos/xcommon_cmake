A pair of application directories with a higher-level CMakeLists.txt which adds both.
Then test that each application can be built on its own from within its application directory
and also that both can be built from the top-level directory.
