Build an application which uses xscope probes; the build will fail if the config.xscope file is not provided to xcc.

app_xscope_src sets the APP_XSCOPE_SRCS variable; app_xscope_glob does not and relies on file globbing.
