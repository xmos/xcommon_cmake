Static library is built for multiple architectures but isn't named. i.e. only
achives for each architecture are built. This is eseentially the same test as
static_lib_arch but relies on some "default" behaviours.
