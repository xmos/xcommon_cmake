Static library has additional source files, one for for each supported language, in a non-default
location. Compilation of the application will fail if these sources are not also compiled.

The archive sources are also located in a non-default location.
